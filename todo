page size argument default
  pass overrided argument
query osmid
  sudo -u postgres psql -c "SELECT osm_id, name FROM planet_osm_polygon WHERE name ILIKE '%...%'" -d maposmatic

install ttf-dejavu
try smaller district
show house numbers
halo on house numbers

readme
  requirements
    linux
    systemd-nspawn
    debootstrap if not debian/ubuntu/arch
  usage
  screenshots
  sample pdf
  credits and resources

remove paddings
links
